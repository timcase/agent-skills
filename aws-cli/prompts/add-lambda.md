I want to extend the aws-cli skill at `/home/tcase/Sites/agent-skills/aws-cli` to add Lambda support. Create a `references/lambda.md` file covering common ad-hoc queries and operations: listing functions, invoking functions, checking logs, viewing configuration, and updating function code. Then update `SKILL.md` to add a service routing table (like the doctl skill uses) that points to `references/s3api.md` and `references/lambda.md`. Package the updated skill when done.
